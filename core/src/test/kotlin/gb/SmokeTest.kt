package gb

import org.junit.jupiter.api.Assertions.assertEquals
import org.junit.jupiter.api.Test

class SmokeTest {
    @Test fun `gradle e junit funcionam`() {
        assertEquals(4, 2 + 2)
    }
}
